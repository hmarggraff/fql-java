subprojects {
  group = 'org.fqlsource'
  version = 1.0
  apply plugin: 'java'
  sourceCompatibility = 1.6
  targetCompatibility = 1.6
  repositories {
    mavenCentral()
  }

  task getlibs << {
    copy {
      from configurations.testCompile
      into 'lib'
    }
  }


}
